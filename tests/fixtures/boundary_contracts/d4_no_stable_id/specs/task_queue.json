{
  "_comment": "Simulates current, un-patched concept-to-code: constraints have no id field (docs/concept-to-code-modifications.md gap #6 not applied upstream yet).",
  "concept": "TaskQueue",
  "constraints": [
    { "english": "pop_ready returns None only when no task has deadline <= now", "logic": "true", "kind": "postcondition", "applies_to": ["pop_ready"] }
  ]
}
